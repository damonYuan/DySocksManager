apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dyssm.fullname" . }}-test-connection"
  labels:
{{ include "dyssm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dyssm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
