apiVersion: v1
kind: Service
metadata:
  name: {{ include "dyssm.fullname" . }}
  labels:
    {{- include "dyssm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dyssm.selectorLabels" . | nindent 4 }}
